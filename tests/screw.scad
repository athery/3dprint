include <../lib/threads.scad>;

$fn=200;
    metric_thread (diameter=7.2, pitch=2, length=5);
translate([15,0,0]) {
//    difference () {
  //     cylinder (5, 6,6);

    //   translate([0,0,-0.1]) metric_thread (diameter=8.5, pitch=2, length=5.2, internal=true);
  //  }
   // metric_thread (diameter=7.5, pitch=2, length=5);
}

//translate([30, 0, 0]) metric_thread (diameter=6.5, pitch=2, length=5);